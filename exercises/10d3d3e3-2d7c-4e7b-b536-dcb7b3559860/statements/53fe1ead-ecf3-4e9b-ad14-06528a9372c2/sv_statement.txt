<p>Det är också möjligt att ändra mer än ett attribut åt gången genom att använda kommatecken (,) för att separera varje <code>kolumnnamn = uttryck</code>-par.</p><h3>Övning</h3>< p> Ändra <code>LAST_NAME</code> för den anställde som har efternamnet <code>'Bull'</code> med <code>'Toro'</code>. <code>EMAIL</code> måste också ändras och ändras från <code>'ABULL'</code> till <code>'ATORO'</code>.</p>