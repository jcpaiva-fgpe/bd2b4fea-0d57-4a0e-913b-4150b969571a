<p>It is also possible to modify more than one attribute at a time, using commas (,) to separate each <code>column_name = expression</code> pair.</p><h3>Exercise</h3><p> Modify the <code>LAST_NAME</code> of the employee that has the surname <code>'Bull'</code> by <code>'Toro'</code>. The <code>EMAIL</code> must also be modified, changing from <code>'ABULL'</code> to <code>'ATORO'</code>.</p>