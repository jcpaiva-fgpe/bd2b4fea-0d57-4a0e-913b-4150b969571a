INSERT INTO EMPLOYEES (EMPLOYEE_ID, FIRST_NAME, LAST_NAME, EMAIL, HIRE_DATE, JOB_ID, SALARY, COMMISSION_PCT, MANAGER_ID, DEPARTMENT_ID)
VALUES (220, 'Alberto', 'Sierra', 'albsierra@carlosiii.es', CURRENT_DATE, 'IT_PROG',
(SELECT MIN_SALARY FROM JOBS WHERE JOB_ID = 'IT_PROG'),
(SELECT MAX(COMMISSION_PCT) FROM EMPLOYEES),
(SELECT MANAGER_ID FROM DEPARTMENTS WHERE DEPARTMENT_NAME = 'IT'),
(SELECT DEPARTMENT_ID FROM DEPARTMENTS WHERE DEPARTMENT_NAME = 'IT')
);