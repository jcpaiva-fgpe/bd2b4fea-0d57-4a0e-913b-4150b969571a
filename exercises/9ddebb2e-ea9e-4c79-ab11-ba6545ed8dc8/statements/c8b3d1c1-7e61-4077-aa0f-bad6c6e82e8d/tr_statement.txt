<p><code>DELETE</code> ifadesinin bir tablodan <strong>tüm satırları silen</strong> bir ifade olduğu konusunda ısrar etmeliyiz, bu nedenle silmek istediğimiz bazı özniteliklerin değeri.</p><h3>Örnek</h3><p>İş tanımlayıcısı <code>'<strong>SA_MAN</strong>'< /code olan çalışanların komisyonunu ortadan kaldıracağız >. Yalnızca bir özelliğin değerini kaldırmak istediğimizden, <code>DELETE</code> ifadesi yerine aşağıdaki gibi bir <code>UPDATE</code> kullanmamız gerekecek:</p><blockquote> <pre ><code>BEGIN;<br />SELECT * WHERE JOB_ID = 'SA_MAN';<br /><br /><strong>Çalışanları GÜNCELLE<br />SET COMMISSION_PCT = NULL<br />JOB_ID NEREDE = ' SA_MAN';</strong><br />--GÜNCELLEME 5<br /><br />SELECT * WHERE JOB_ID = 'SA_MAN';<br />ROLLBACK;</code></pre> </ blockquote><h3>Alıştırma</h3><p>114 numaralı çalışanın görevi sona ermiştir, bu nedenle ona hiyerarşik bir üst olarak sahip olan herkes, kendilerine bir hiyerarşik üst atanana kadar hiyerarşik bir üst olmadan bırakılır.</p ><p>Bu nedenle, hiyerarşik üstleri olarak çalışan 114'e sahip olanlardan <code>MANAGER_ID</code> özniteliğinin değerini kaldırır.</p>