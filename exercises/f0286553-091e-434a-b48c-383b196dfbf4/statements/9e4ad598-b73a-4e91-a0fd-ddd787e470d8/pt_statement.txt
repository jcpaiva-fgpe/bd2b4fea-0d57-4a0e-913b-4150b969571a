<p>Ao fazer um <code><em>INSERT</em></code> no qual os valores de todos os campos não estão especificados, o valor <code><em>NULL</em>< será ser obtido. /code> naqueles campos que não foram indicados.</p><h3>Exemplo:</h3><p>Vamos fornecer valores apenas naqueles atributos que não permitem o valor NULL :</p><blockquote> <pre><code>BEGIN;<br /><strong>INSERT INTO EMPLOYEES (EMPLOYEE_ID, LAST_NAME, EMAIL, HIRE_DATE, JOB_ID)<br />VALUES (220, 'myLastName', ' email@juezlti.eu', CURRENT_DATE , 'IT_PROG');</strong><br />-- INSERT 0 1</code></pre></blockquote><p>Vamos ver qual é o resultado da inserção em a tabela de funcionários é.</ p><blockquote><pre><code>SELECT * FROM EMPLOYEES WHERE EMPLOYEE_ID = 220;<br />ROLLBACK;</code></pre></blockquote><blockquote><pre > funcionário_id | first_name | sobrenome | e-mail | número_telefone | data_contratação | job_id | salário | comissão_pct | manager_id | departamento_id <br />-------------+------------+------------+----- -------------+--------------+--------------------- +---------+--------+----------------+------------+ ---------------<br /> 220 | | meuSobrenome | email@juezlti.eu | | 2022-08-31 00:00:00 | IT_PROG | | | | <br />(1 linha)</pre></blockquote><p>Podemos verificar se todos os campos que não foram indicados no <code>INSERT</code> aparecem com o valor <code>NULL< /code>.</p><p>Se a lista de campos indicados não corresponder à lista de valores, ou se nenhum valor for fornecido para campos que não permitem o valor <code>NULL</code> , será retornado um erro. erro na execução.</p><h3>Exemplo:</h3><p>Se o campo Sobrenome não for indicado, mas for especificado um valor para o referido campo:</p><blockquote ><p><code >INSERT INTO EMPLOYEES (EMPLOYEE_ID, EMAIL, HIRE_DATE, JOB_ID)<br />VALUES (220, 'myLastName', 'email@juezlti.eu', CURRENT_DATE, 'IT_PROG');</code> </p><p ><strong><code>-- ERRO: INSERT tem mais expressões do que colunas de destino</code></strong></p></blockquote><h3>Exercício:</h3>< p>A instrução a seguir gerará o erro acima, corrija-o para que a inserção possa ser concluída sem erros.</p><blockquote><p><code>INSERT INTO locais (LOCATION_ID, STREET_ADDRESS)<br />VALUES (3300 , 'C/Carlos II I, 3', 'Cartagena');</code></p></blockquote><p>Adicione o mínimo necessário para que a instrução seja executada corretamente.</p>