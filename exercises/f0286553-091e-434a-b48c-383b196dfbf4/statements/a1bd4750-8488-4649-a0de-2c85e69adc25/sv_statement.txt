<p>När du gör en <code><em>INSERT</em></code> där värdena för alla fält inte anges, kommer värdet <code><em>NULL</em>< att erhållas. /code> i de fält som inte har angivits.</p><h3>Exempel:</h3><p>Vi kommer endast att tillhandahålla värden i de attribut som inte tillåter NULL-värdet :</p><blockquote> <pre><code>BEGIN;<br /><strong>INSERT I ANSTÄLLDA (EMPLOYEE_ID, LAST_NAME, EMAIL, HIRE_DATE, JOB_ID)<br />VÄRDEN (220, 'myLastName', ' email@juezlti.eu', CURRENT_DATE , 'IT_PROG');</strong><br />-- INSERT 0 1</code></pre></blockquote><p>Låt oss se vad resultatet av att infoga i medarbetartabellen är.</ p><blockquote><pre><code>VÄLJ * FRÅN ANSTÄLLDA WHERE EMPLOYEE_ID = 220;<br />ROLLBACK;</code></pre></blockquote><blockquote><pre > anställd_id | förnamn | efternamn | e-post | telefonnummer | anställningsdatum | jobb_id | lön | provision_pct | manager_id | department_id <br />-------------+------------+------------+----- --------------+---------------------+--------------------- +--------+--------+----------------+------------+ ---------------<br /> 220 | | mitt efternamn | email@juezlti.eu | | 2022-08-31 00:00:00 | IT_PROG | | | | <br />(1 rad)</pre></blockquote><p>Vi kan verifiera att alla fält som inte har angivits i <code>INSERT</code> visas med värdet <code>NULL< / code>.</p><p>Om listan med angivna fält inte motsvarar värdelistan, eller om inga värden tillhandahålls för fält som inte tillåter <code>NULL</code>-värdet , kommer ett fel att returneras. fel i exekveringen.</p><h3>Exempel:</h3><p>Om fältet Efternamn inte anges men ett värde anges för fältet:</p><blockquote ><p><code >INSERT I ANSTÄLLDA (EMPLOYEE_ID, EMAIL, HIRE_DATE, JOB_ID)<br />VÄRDEN (220, 'my LastName', 'email@juezlti.eu', CURRENT_DATE, 'IT_PROG');</code> </p><p ><strong><code>-- FEL: INSERT har fler uttryck än målkolumner</code></strong></p></blockquote><h3>Övning:</h3>< p>Följande sats kommer att generera ovanstående fel, fixa det så att infogningen kan slutföras utan fel.</p><blockquote><p><code>INSERT INTO platser (LOCATION_ID, STREET_ADDRESS)<br />VALUES (3300) , 'C/Carlos II I, 3', 'Cartagena');</code></p></blockquote><p>Lägg till det minimum som krävs för att satsen ska köras korrekt.</p>