<p>When doing an <code><em>INSERT</em></code> in which the values ​​of all the fields are not specified, the value <code><em>NULL</em>< will be obtained. /code> in those fields that have not been indicated.</p><h3>Example:</h3><p>We are going to provide values ​​only in those attributes that do not allow the NULL value:</p><blockquote> <pre><code>BEGIN;<br /><strong>INSERT INTO EMPLOYEES (EMPLOYEE_ID, LAST_NAME, EMAIL, HIRE_DATE, JOB_ID)<br />VALUES (220, 'myLastName', 'email@juezlti.eu', CURRENT_DATE , 'IT_PROG');</strong><br />-- INSERT 0 1</code></pre></blockquote><p>Let's see what the result of inserting into the employee table is.</ p><blockquote><pre><code>SELECT * FROM EMPLOYEES WHERE EMPLOYEE_ID = 220;<br />ROLLBACK;</code></pre></blockquote><blockquote><pre> employee_id | first_name | last_name | e-mail | phone_number | hire_date | job_id | salary | commission_pct | manager_id | department_id <br />-------------+------------+------------+----- -------------+--------------+--------------------- +---------+--------+----------------+------------+ ---------------<br /> 220 | | myLastName | email@juezlti.eu | | 2022-08-31 00:00:00 | IT_PROG | | | | <br />(1 row)</pre></blockquote><p>We can verify that all the fields that have not been indicated in the <code>INSERT</code> appear with the value <code>NULL</ code>.</p><p>If the list of indicated fields does not correspond to the list of values, or if no values ​​are provided for fields that do not allow the <code>NULL</code> value, an error will be returned. error in the execution.</p><h3>Example:</h3><p>If the Surname field is not indicated but a value is specified for said field:</p><blockquote><p><code >INSERT INTO EMPLOYEES (EMPLOYEE_ID, EMAIL, HIRE_DATE, JOB_ID)<br />VALUES (220, 'myLastName', 'email@juezlti.eu', CURRENT_DATE, 'IT_PROG');</code></p><p ><strong><code>-- ERROR: INSERT has more expressions than target columns</code></strong></p></blockquote><h3>Exercise:</h3><p>The following statement will generate the previous error, fix it so that the insert can complete without error.</p><blockquote><p><code>INSERT INTO locations (LOCATION_ID, STREET_ADDRESS)<br />VALUES (3300, 'C/Carlos II I, 3', 'Cartagena');</code></p></blockquote><p>Add the minimum necessary for the statement to execute correctly.</p>