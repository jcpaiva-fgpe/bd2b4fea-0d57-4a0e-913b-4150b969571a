<p>In any <code>DML</code> statement we can use, as values ​​to assign to the attributes, more complex expressions than those achieved with constant values, like the one in the previous exercise.</p><h3> Exercise</h3><p>Use <a href="https://www.postgresql.org/docs/14/functions-datetime.html" target="_blank">date arithmetic</a> to update the hire dates (HIRE_DATE) of the employees of the department whose identifier is 60, incrementing them in an interval of 12 years.</p>