<p><code>SQL</code> dili, bir veritabanında depolanan verileri düzenlemek (eklemek, güncellemek ve silmek) için bir dizi ifadeye sahiptir. Bu ifade grubuna <code><em>Veri Manipülasyon Dili</em> (DML)</code> adı verilir.</p><p><a href=”https://www.postgresql .org ifadesi /docs/14/sql-insert.html” target=”_blank”><code>INSERT</code></a>, mevcut bir tabloya yeni satırlar veya kayıtlar eklenmesine izin verir.</p><h3 >Sözdizimi</p><h3</p> h3><p><code>INSERT</code> ifadesini kullanmanın en basit biçimi aşağıdaki sözdizimine sahiptir:</p><blockquote><pre><code>INSERT INTO <em>table_name </em> [ ( <em>column_name</em> [, ...] ) ]<br /> DEĞERLER ( <em>ifade</em> [, ...] );</code></ pre></blockquote> <p>Burada <em>tablo_adı</em>, yeni kayıtlar eklemek istediğiniz tablonun adı olacaktır. <code>( <em>column_name</em> [, ...] )</code> içinde yeni değerleri yazmak istediğiniz söz konusu tablonun sütunları <code>DEĞERLER ( <em> ifade</em> [, ...] )</code>. <code>( <em>column_name</em> [, ...] )</code> sütun listesini, tüm sütunların değerleri sağlanmışsa ve oldukları sıraya göre atlamak mümkündür. tabloda bulunur.</p><p>Hem sütun listesi hem de değerler listesi, öğelerinin her birini virgülle ayıracaktır. Ayrıca, <code>( <em>column_name</em> [, ...] )</code> öğesinin her alanının, <code>VALUES ( <em >expression</em>) içindeki karşılık gelen konumda geçerli bir değere sahip olması gerektiğini unutmayın. em> [, ...] )</code> (Her alan için geçerli değerleri hatırlamıyorsanız, ardından gelen <code><em>\d</em></code> cümlesini kullanabilirsiniz. sorgulamak istediğiniz tablonun adıyla).</p><h3>Örnek</h3><p>Ülkeler listesinin arasına <code>İspanya</code> ekleyerek başlayalım.</p>< blockquote><p ><code>BAŞLAT;<br /><br /><strong>ÜLKELERE GİRİN (COUNTRY_ID, COUNTRY_NAME, REGION_ID)<br />DEĞERLER ('ES', 'İspanya', 1);</ güçlü><br />-- INSERT 0 1<br /><br />Ülkelerden * SEÇİN;<br />ROLLBACK;</code></p></blockquote><h3>Alıştırma</h3> <p>Aşağıdaki verilerle <code>'Web Geliştirme'</code> departmanını oluşturun:</p><table align="center" border="1" Summary="Sorgu 1"><tbody> <tr>< th style="background-color:#9e9e9e">DEPARTMENT_ID</th><th style="background-color:#9e9e9e">DEPARTMENT_NAME</th><t h style="background-color:#9e9e9e">MANAGER_ID</th><th style="background-color:#9e9e9e">LOCATION_ID</th></tr><tr><td>280</td> <td>Web Geliştirme</td><td>103</td><td>1700</td></tr></tbody></table><small><p><strong>Not:</td></tr></tbody></table><small><p><strong>Not:</td><td> güçlü> <code>JuezLTI</code>'de <strong>her yanıt başlatıldığında, veriler sıfırlanır.</strong> Bu nedenle, <code>PostgreSQL</code> çalışma ortamınızda sahip olmanız önemlidir. her zaman <code>HR</code> şemasının ilk verileridir.</p><p>Bunu yapmak için, örnekte de görebileceğiniz gibi, her etkinliğe <code>BEGIN;</code> ile başlamalısınız. , hangi bir işlem başlatır. Ardından, istediğiniz kadar ifade çalıştırabilirsiniz, örneğimizde bir <code>INSERT</code> ve bir <code>SELECT</code> başlattık.</p><p>Bir etkinlik bittiğinde <code>ROLLBACK;</code></p><p>Ancak, <a aktarılması gereken tek ifade ile <code>HR</code> başlangıç ​​değerleri ile şemadan tekrar ayrılacağız. code>JuezLTI</code> Vurgulanan bu olacaktır. Bizim durumumuzda, <code>INSERT</code>'u <code>JuezLTI</code>'ye taşımamız gerekecek.</p></small>